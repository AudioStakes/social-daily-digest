{
  "name": "social-daily-digest",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "macOS-only local CLI that fetches X home timeline via X API and generates a daily Markdown digest",
  "bin": {
    "sns-digest": "dist/main.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit -p tsconfig.json",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "nodemailer": "^8.0.6",
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "@types/node": "^24.9.0",
    "typescript": "^5.9.3"
  }
}
